Reconnect to your instance and use the ifconfig command to verify that the IPv6 address is configured on the network interface.Alternatively, you can use the following procedure to modify the /etc/sysconfigbetwork-scripts/ifcfg-eth0 file directly. You must use this method with earlier version of RHEL and CentOS that don't support cloud-init.To configure a RHEL/CentOS instanceConnect to your instance using the instance's public IPv4 address.Using a text editor of your choice, open /etc/sysconfigbetwork-scripts/ifcfg-eth0 and locate the following line: IPV6INIT="no" Replace that line with the following: IPV6INIT="yes" Add the following two lines, and save your changes: DHCPV6C=yesNM_CONTROLLED=no