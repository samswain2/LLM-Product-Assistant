AWS PrivateLink service supports one type of resource-based policy, known as an endpoint policy. An endpoint policy controls which AWS principals can use the endpoint to access the endpoint service. For more information, see Control access to VPC endpoints using endpoint policies.Policy actions for AWS PrivateLinkSupports policy actions YesAdministrators can use AWS JSON policies to specify who has access to what. That is, which principal can perform actions on what resources, and under what conditions.