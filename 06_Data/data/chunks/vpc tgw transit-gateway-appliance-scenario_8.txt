10.0.0.0/16 local 0.0.0.0/0 tgw-idVPC CThe shared services VPC (VPC C) has different route tables for each subnet. Subnet A is used by the transit gateway (you specify this subnet when you create the VPC attachment). The route table for subnet A routes all traffic to the appliance in subnet B.192.168.0.0/16 local 0.0.0.0/0 appliance-eni-idThe route table for subnet B (which contains the appliance) routes the traffic back to the transit gateway.