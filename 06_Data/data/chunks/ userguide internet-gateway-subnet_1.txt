Creates the following route tables: A route table for the internet gateway A route table for the destination subnet A route table for the middlebox subnetAdds the necessary routes to the new route tables as described in the following sections.Disassociates the current route tables associated with the internet gateway, subnet B, and subnet C.Associates route table A with the internet gateway (the Source in the middlebox routing wizard), route table C with subnet C (the Middlebox in the middlebox routing wizard), and route table B with subnet B (the Destination in the middlebox routing wizard).