For routes with the same destination IP address but different targets, the route priority is as follows: Static routes (for example, Site-to-Site VPN static routes) Prefix list referenced routes VPC propagated routes Direct Connect gateway propagated routes Transit Gateway Connect propagated routes Site-to-Site VPN propagated routes Transit Gateway peering propagated routes (Cloud WAN)