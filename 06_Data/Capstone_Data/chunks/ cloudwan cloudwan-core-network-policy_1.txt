Create a core network while creating a global networkTo create a core network while creating a global networkPrerequisite: Create a global network.Create the core network. See Create a global network.On the Create core network page, under Core network general settings, enter a Name and Description to identify the core network.(Optional) Choose Additional settings to add one or more Key and Value tags to help identify this network resource.