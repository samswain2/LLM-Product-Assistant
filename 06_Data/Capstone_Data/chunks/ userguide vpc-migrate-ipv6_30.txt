If you're using Ubuntu Server 14, you must include a workaround for a known issue that occurs when restarting a dual-stack network interface (the restart results in an extended timeout during which your instance is unreachable).These steps must be performed as the root user.To configure an Ubuntu Server 14 instanceConnect to your instance using the instance's public IPv4 address.Edit the /etcbetwork/interfaces.d/eth0.cfg file so that it contains the following:auto loiface lo inet loopbackauto eth0iface eth0 inet dhcp up dhclient -6 $IFACEReboot your instance:sudo rebootReconnect to your instance and use the ifconfig command to verify that the IPv6 address is configured on the network interface.Start the DHCPv6 client