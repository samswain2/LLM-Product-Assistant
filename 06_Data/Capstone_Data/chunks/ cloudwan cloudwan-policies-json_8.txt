segments — At least one segment must be defined and composed of the following parameters: name — The name of the segment. The name is a string used in other parts of the policy document, as well as in the console for metrics and other reference points. Valid characters are a–z, A–Z, and 0–9. NoteThere is no ARN or ID for a segment. description — (Optional) A user-defined string describing the segment. edge-locations — (Optional) Allows you to define a more