When a service provider creates a VPC endpoint service, AWS generates an endpoint-specific DNS hostname for the service. These names have the following syntax:The following is an example of a DNS hostname for a VPC endpoint service in the us-east-2 Region:When a service consumer creates an interface VPC endpoint, we create Regional and zonal DNS names that the service consumer can use to communicate with the endpoint service. Regional names have the following syntax:Zonal names have the following syntax:Private DNS