For Policy, select Full access to allow all operations by all principals on all resources over the VPC endpoint. Otherwise, select Custom to attach a VPC endpoint policy that controls the permissions that principals have to perform actions on resources over the VPC endpoint.(Optional) To add a tag, choose Add new tag and enter the tag key and the tag value.Choose Create endpoint.To create a gateway endpoint using the command linecreate-vpc-endpoint (AWS CLI)New-EC2VpcEndpoint (Tools for Windows PowerShell)Control access using IAM policiesYou can create IAM policies to control which IAM principals can access DynamoDB tables using a specific VPC endpoint.Example: Restrict access to a specific endpoint