For actions that don't support resource-level permissions, such as listing operations, use a wildcard (*) to indicate that the statement applies to all resources.The VPC resource has the ARN shown in the following example.{Partition}:ec2:${Region}:${Account}:vpc/${VpcId}For example, to specify the vpc-1234567890abcdef0 VPC in your statement, use the ARN shown in the following example.To specify all VPCs in a specific Region that belong to a specific account, use the wildcard (*).Some Amazon VPC actions, such as those for creating resources, cannot be performed on a specific resource. In those cases, you must use the wildcard (*).Many Amazon EC2 API actions involve multiple resources. To specify multiple resources in a single statement, separate the ARNs with commas.