This document is about using AWS Identity and Access Management (IAM) to control access to AWS Network Manager and Infrastructure Performance resources. It explains how IAM administrators can use IAM to secure AWS resources and provides instructions on allowing IAM users or groups to access Infrastructure Performance. It also mentions the importance of using IAM users instead of AWS account credentials for increased security.